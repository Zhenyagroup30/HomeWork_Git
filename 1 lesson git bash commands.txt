	clear или ctrl+l - очистка экрана﻿

i- отменяет рекистр

ls -la  - вывести список файлов и папок
ls -l - файлы в той папке, где сейчас находитесь
pwd - показывает каталог, где вы находитесь
clear - очистить терминал
mkdir создать папку
touch - создать файл (после последней строки обязательно нажать Enter)
cat > "dir name"/"file_name.*" - создание файла сразу в выбранной деректории, с последующим редактированием
cd ..  - на "уровень" назад (пердыдущий каталог)
cd ../ ../ - на 2 "уровня" назад и так далее...
cd название папки - переход в нужную папку
crtl + c - закрытие редактирования
find . название файла (поиск файла по имени)
cd *dir name* + tab - для вывода названий папок которые начинаются с чего-то

копирование сразу нескольких файлов:
cp qa_1/{qq4.json,qq5.json} qa_2


command "grep"
grep что ищем название файла (поиск слова в файле) РЕГИСтроЗАВИСИМЫЙ
grep -i что ищем название файла (регистро неЗавИсимый)
grep -v "х" название файла (найдет все кроме "х")
grep -n "x" название файла (поиск с нумерацией строк)
grep -c "x" qq.txt колличество строк, которые содержат "х" (регистрозависимой)
grep -ic "x" qq.txt //-//-// (регистронезависимо)
grep "х" название файла (поиск "х" в файле)
grep "x" ./* поиск во всех папках
grep -iwn -A "cat" qq.txt > "название файла" (сохранить результат поиска в файл)
grep [A-Za-z].txt qq.txt (поиск всех совпадений с расширением .txt)

поиск ТОЛЬКО НУЖНОГО текста

grep -nw "sec" *

ПОИСК В ДАННОЙ ПАПКЕ (в которой находитесь) в конце звездочка (*)


cd - выйти в корень

Редактирование создание файла через "cat"

cat >> "file name.*" редактирование файла с добавлением информации, не удаляя старой
cat > "file name.*" редактирование файла с "заменой" старой информации
cat название файла - просмотр содержимого

history - history of request  ( "q" for quit)

good job

hello world

new string  !!!!


vim название файла - редактор файлов (
при входе нажать i, после редактирования esc, потом :wq enter
если сохранять не нужно, то :q! )

перемещение файлов:
mv qa_1/file.txt qa_3/file.txt (перемещение файлов (mv qa_1 (откуда) txt.txt (какой файл)пробел qa_3/txt.txt (куда и какое название)

переименовать файл
mv file.txt 111.txt
mv qq.txt .. (перенос файла на папку "выше")

копирование файлов:
cp qa_3/txt.txt qa_1/txt.txt (копирование файла)



cat название папки/название документа (просмотр файла в любой папке)
rm название файла (удаление файла)
rm -r (название папки) - удаление папки
rmdir (название папки) удаление папки

rm -r название папки - удаление !!!!не пустой папки, если в ней находяться данные!!!!
